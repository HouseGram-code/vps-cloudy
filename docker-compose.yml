services:
  discord-vps-bot:
    build: .
    container_name: discord-vps-bot
    restart: unless-stopped
    env_file: .env
    # The bot manages Docker containers ("VPS"), so it needs the Docker socket.
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
