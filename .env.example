# Discord bot token — DO NOT commit the real token to GitHub.
DISCORD_TOKEN=PASTE_YOUR_TOKEN_HERE

# VPS defaults (Docker resource limits)
NODE_NAME=Local Node
OWNER_PREFIX=tbmen12
DEFAULT_RAM=1g
DEFAULT_CPU=1
DEFAULT_DISK=10g
LIFETIME_DAYS=30
